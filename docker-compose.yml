services:
  app:
    build:
      context: .
      args:
        SSI_API_KEY: ${SSI_API_KEY}
    ports:
      - "3000:3000"
    environment:
      SSI_API_KEY: ${SSI_API_KEY}
    restart: unless-stopped
